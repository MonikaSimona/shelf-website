import { Icon } from "@iconify/react";
import React from "react";

interface Props {}

const Navbar = (props: Props) => {
  return (
    <div>
      <Icon icon="bx:bxs-camera-movie" />
    </div>
  );
};

export default Navbar;
